for ascii in range(65,132):
    print(ascii, '\t', chr(ascii))

#traversing strings#
for achar in "Sept":
    print(achar)

month = "Sept"
for idx in range(len(month)):
    print("month[", idx, "] = ", month[idx])
